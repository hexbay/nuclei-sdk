id: ratelimit-timing

info:
  name: ratelimit timing
  author: tests
  severity: info

http:
  - method: GET
    path:
      - "{{BaseURL}}/"
    matchers:
      - type: status
        status:
          - 200
